DO $$ DECLARE r RECORD; BEGIN FOR r IN (SELECT c.relname AS seq_name FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace WHERE c.relkind = 'S' AND n.nspname = 'public' AND c.relname LIKE '%_id_seq') LOOP EXECUTE 'DROP SEQUENCE IF EXISTS public.' || quote_ident(r.seq_name) || ' CASCADE;'; END LOOP; END $$;
